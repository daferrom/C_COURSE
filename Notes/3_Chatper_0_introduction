# Chapter 0

- C is general-purpose programming Language.
- Closely associated to Unix System
- Not tied to a any one operating system or machine-
- a System programming Language
- useful for wrtieen OS

-C relatively low language

- The lack of heap or "garbage collection" feature feature C is one of the great strengths of the language at the same time is likely the reason that the average programmer will never develop or mantain a major C application during their career.


- C provides malloc() and free() that provides a certain amount of memory.
IS the determine the amount of memory in advance to some operation and then returned to the C runtime library. e,g for converting a JPG image in a PNG>

- The term "heap" refers to memory  tha manages on our behalf when we need to "borrow" a bit of memory and give it back later.

- If we forget to call free() when we are done with a memory, we created a "memory leak" and our program eventually will run out of memory and abort.

- C places the onus of giving back any dynamically allocated memory on the programmer.

- Modern languages like Java, Javascript, and Python keep track of when we stop dynamic memory layer that can automatically reclaim the memory.

- The more difficult problem after a series of calls to malloc() and free() the heap space becomes fragmented and the clean up is needed. The Clean up is called "garbage collection".

- Kerningham and Ritchie knew "garbage collection" was difficult. So they left it out of the C language and put it a run-time library 

- C is relatively C
- Can be described i a smal lspace.
- and learned quickly.
- A compiler of C cna be simple and compact.

- C programs tend to be efficient enough that there is no compulsion to write assembly language instead. The most obvious example of this is the UNIX OS which is entirely written in C.

- only 800 at the very lowest level are in assembler.

- The creators of C and UNIX were advocating for minimal set of programming language constructs and more reliance on calling functions in provided runtime libraries to meet programmers use case.

-It is independent of any machine architecture.

- Many of the most important ideas of C stem from the considerably older, but still quite vital, language BCPL, developed by Martin Richards. Proceeded indirectly from B which was written by ken Thompson.

- In C fundamental data objects are characters, integers of several sizes, and floating point numbers.
- There is a hierarchy of derived data types created with pointers, arrays, structures, unions and functions

- C provides the fundamental flow constructions:
decission making if
looping wit ha termination test at the top (while, for) 
or at the bottom (do) and
selecting one (swithc)
BCPL offer those tho with different syntax anticipating structure programming by several years.

- C provides pointers and the ability to do address arithmetic. 
- The arguments to functions are passed by copying the value of the arguments, and its impossible for the called function to change the actual argument in the caller .















